extends layout

block content
  article#leaguesContainer 
     
     section#leagueList.box
        p.blurb
           em Click <span class="standings"></span> for current standings
        h2 Germany
        each league in leaguedata
         //- p.id(data-leagueid= league.id data-leaguecode= league.code)
          if league.code === 'BL1'
            h3
               a(data-league-id= league.id href='/teams?id='+league.id)= league.name
            //-    a(data-url= league._links.teams.href, href='/teams?dataurl='+league._links.teams.href+'&league='+league.caption+'&season='+season)= league.caption
            //- a.standings(data-standings= league._links.leagueTable.href, href= '#', title= 'standings') 
          
        h2 Britain
        each league in leaguedata
          if league.code === 'PL'
            h3
               a(data-league-id= league.id href='/teams?id='+league.id)= league.name
            
        h2 France
        each league in leaguedata
          if league.code === 'FL1'
            h3
               a(data-league-id= league.id href='/teams?id='+league.id)= league.name
        
        h2 Spain
        each league in leaguedata
          if league.code === 'PD'
            h3
               a(data-league-id= league.id href='/teams?id='+league.id)= league.name

        h2 Netherlands
        each league in leaguedata
          if league.code === 'DED'
            h3
               a(data-league-id= league.id href='/teams?id='+league.id)= league.name
            
        h2 Portugal
        each league in leaguedata
          if league.code === 'PPL'
            h3
               a(data-league-id= league.id href='/teams?id='+league.id)= league.name
        
        p.note
           em Click a league name to see the teams and standings
        
        // need to do Champions League differently - api has no standings for it
        // h2 Europe
        // each league in leaguedata
          // if league.league === 'CL'
            h3
               a(data-url= league._links.teams.href, href='/teams?dataurl='+league._links.teams.href+'&league='+league.caption+'&season='+season)= league.caption
            a.standings(data-standings= league._links.leagueTable.href, href= '#', title= 'standings') 
                  

